# zisaze-toss 세션 v5 (2026-07-07)

정본 계획서 `구현계획서_v2.md`, 실기기 프로토콜 `실기기테스트_P3-3_v1.md`, 검수 메모 `검수메모_P3-4_v1.md`, 이전 세션 `zisaze-toss_session_v4.md`. 이 문서는 인계용.

## 한 줄 요약
**P3-2 번들 업로드·P3-3 실기기 검증(R1 게이트) 통과.** 콘솔 앱 정보(로고·썸네일·세로 스크린샷 3장·키워드)까지 등록·저장 완료. 남은 건 사용자 직접의 **검토 요청 제출**(체크박스 5개 + 검수메모 붙여넣기 + 버튼)뿐.

## 이번 세션에 한 일 — 콘솔 자동 제어(브라우저 오케스트레이션)

사용자 지시: "네가 직접 제어해서 업로드해줘." → chrome-devtools MCP로 앱인토스 콘솔을 직접 조작.

### 0. 환경 복구
- MCP 전용 크롬(프로필 `~/.cache/chrome-devtools-mcp/chrome-profile`)이 떠 있는데 서버가 연결 핸들을 잃어 "이미 실행 중"으로 붙지 못함(파이프 방식 재연결 불가). → **그 프로필 크롬 프로세스만 kill 후 재실행**하니 붙음. **로그인은 디스크 프로필에 남아 유지**됨(재로그인 불필요).
- 콘솔: `apps-in-toss.toss.im` (개발자센터 `developers-apps-in-toss.toss.im`). 로그인 유지 확인.

### 1. 앱/번들 (P0-2·P3-2)
- **워크스페이스 50627**(김철희, 멤버 1명), **미니앱 지사제 = id 50557**, appName `zisaze`(불변) — 앱은 **이미 등록돼 있었음**(생활>비즈니스>사장님).
- **번들 `zisaze.ait` 업로드 → 버전 `20260707-1`, 빌드 완료(SDK 2.10.4), 상태 "검토 필요".** 메모 "P3-2 샌드박스/실기기 테스트용 첫 빌드 (MVP)".
- **샌드박스 QR 생성** → `brand/console/_sandbox_QR_20260707-1.png`로 저장(딥링크 `intoss-private://zisaze?_deploymentId=019f3768-aaa3-7b6d-9bc8-2d2372441770`). "푸시 보내기"로도 테스트 가능.

### 2. 앱 정보(P3-4 콘솔 등록) — 등록·임시저장 완료
- 기본 정보(한/영 이름·부제·상세설명·appName·연령·이메일)는 기존 입력 유지.
- **로고 600×600 + 다크로고 600×600 + 썸네일 1932×828** 업로드.
- **세로 스크린샷 3장(636×1048, 실제 앱 화면 캡처, 서로 다른 3장)** 업로드.
- **검색 키워드**: 지원사업·정부지원금·소상공인.
- **임시저장** 반영 확인(새로고침 후에도 유지).

### 3. 실기기 검증(P3-3) — **R1 게이트 통과**
- **Android C1(토스 삭제→재설치→같은 계정→재진입): 북마크 유지** = hash 영속 실증 → **R1 통과**.
- **iPhone: 정상 작동 확인.** 처음 "번들 못 찾음"은 계정 문제(샌드박스는 멤버=김철희 계정만 접근)로, 계정 정리 후 해결.

### 4. 스크린샷 세로/가로 이슈 — 원인 규명 및 해결
- 증상: 가로 이미지(1504×741)를 "추가하기"에 넣으면 "이미 세로형 이미지를 모두 추가했다"며 거부.
- **원인(사용자 테스트로 확정): 스크린샷 업로더는 세로/가로 "택일" 방식.** 먼저 올린 방향으로 고정되고 반대 방향은 안 받음. 라벨 "세로형 최소 3장 **/** 가로형 최소 1장"의 "/"가 **OR**. (순수 1504×741 테스트 이미지도 거부돼 우리 배너 파일 문제 아님을 검증.)
- **결정: 실캡처 세로 3장으로 요건 충족, 가로 배너 미사용.** → 사용자의 원래 우려("가로 배너는 실제 화면이 아니다")도 자연 해소. 제출물 전부 실제 앱 화면.
- 규정 확인(공식): 가로형은 실캡처 불필요(비게임=자체제작 홍보 이미지 허용), 단 "텍스트 과다 금지·로고나 짧은 문구만" 권장.

## 남은 일 (전부 [사용자] — 제출 직전 정지)
1. **검토 요청 제출**(콘솔 앱 정보 편집 화면 하단):
   - 확인 체크박스 5개 체크(본인 서약: 오픈가능·자금세탁아님·인허가완료·위법없음·책임부담).
   - 검수 참고사항란에 `검수메모_P3-4_v1.md`의 "콘솔 붙여넣기용" 블록 붙여넣기.
   - **"검토 요청하기" 클릭** (AI 금지 유지 — 사용자 직접).
2. (참고) "검토 요청하기" 비활성은 **가로 이미지 부재가 아니라 체크박스 미체크 때문.** 체크하면 활성. 만약 체크 후에도 "스크린샷 더 필요"로 막히면 → 그때는 정말 둘 다 필요하다는 뜻이니 별도 방법 검토(사용자가 알려주기).

## 주의사항
- **스크린샷 업로더는 세로/가로 택일** — 둘 다 넣으려 하지 말 것. 세로 3장 유지가 현재 정답.
- **콘솔 이미지 업로드 자동화 팁**: 토스 콘솔 업로드는 radix/react-dropzone. `upload_file`이 버튼에 대해 **1회 실패→2회차 성공** 패턴 잦음. 숨은 `input[type=file]`을 JS로 노출(aria-label 부여)해 uid를 잡아 직접 겨냥하면 확실. 단 **택일 제약은 자동화로 못 넘음**(앱 로직).
- **MCP 크롬 "이미 실행 중" 오류**: `chrome-devtools-mcp\chrome-profile` 쓰는 chrome 프로세스만 kill 후 재시도. 로그인은 프로필로 유지됨.
- 좌표: workspace `50627` / mini-app `50557` / 콘솔 앱 출시 `/app-build`, 앱 정보 `/meta`, 편집 `/meta/edit?step=1`.
- `brand/console/_sandbox_QR_20260707-1.png`는 임시 산출물(deploymentId 포함) — 삭제 무방.
- `brand/console/screenshot_landscape_1_1504x741.png`는 이번 결정으로 **미사용**(보관은 무방, 콘솔엔 안 올림).
- scratchpad `test_landscape_1504x741.png`·`test_wide_1504x600.png`는 진단용 임시(자동 폐기 대상).
- rich1 이번 세션 무변경. dev 사용자·npm audit 31건 등 기존 상태 v4와 동일.

## 다음 단계
1. [사용자] 검토 요청 제출(위 남은 일 1).
2. [사용자] 반려 시 사유 공유 → AI가 분석·수정. 스크린샷 관련 반려면 세로/가로 정책 재점검.
3. [AI] (제안) 콘솔 앱 정보에 남은 선택 항목(있으면) 보완은 요청 시.
