# 지사제 앱인토스(토스 미니앱) 등록 — 구현계획서 v1

> 작성: 2026-07-05 (Claude Fable 5, 사전조사 세션)
> 실행 주체: goal 모드 + Opus 4.8 (이 문서가 실행 에이전트의 단일 진입점)
> 사용자 결정 반영: ① 리치앤택스 배너는 미니앱에 없어도 됨 ② 정부지원사업 카테고리는 토스에 이미 존재함을 사용자가 확인 ③ 기존 웹서비스(zisaze.com)는 절대 영향받지 않아야 함
> 근거 문서: `hamtopia/docs/앱인토스_제약사항.md`, `탱탱볼해금/앱인토스_게임개발_가이드_정리.md`, 토스 공식 문서(intro/guide, checklist/app-nongame, tutorials/webview, getAnonymousKey, openURL — 2026-07-05 직접 확인)

---

## 0. 실행 에이전트 필독 규칙 (모든 Phase에 우선)

1. **웹 무중단이 최우선.** rich1(기존 웹) 변경은 이 문서 §5 Phase 1에 명시된 **추가(additive) 작업 3종만** 허용한다. 기존 라우트·컴포넌트·테이블 컬럼의 수정/삭제 금지. 명시되지 않은 rich1 변경이 필요해지면 **작업을 멈추고 사용자에게 보고**한다.
2. rich1을 건드리기 전에 `rich1/docs/AI_ONBOARDING.md`를 먼저 읽는다(rich1 CLAUDE.md 규칙). rich1 커밋은 dev 브랜치 → 검증 → main. 커밋 후 push.
3. 미니앱(신규 폴더 `zisaze-toss/`)은 자유롭게 개발하되, §3 토스 하드 제약을 위반하는 코드는 절대 작성하지 않는다. 코드 수정 후마다 §3 셀프체크를 돌린다.
4. **토스 콘솔의 "검토 요청"·"출시하기" 버튼은 절대 에이전트가 누르거나 누르라고 재촉하지 않는다.** 사용자가 직접 결정한다(출시는 즉시 100% 반영·롤백도 즉시라 비가역성 큼).
5. 비밀(API 키·토큰)은 `.env`에만. 코드에 하드코딩 금지.
6. 토스 공식 문서는 수시로 바뀐다. SDK 함수명·설정이 이 문서와 다르면 **공식 문서(developers-apps-in-toss.toss.im)가 정본**이다. 대부분 페이지는 URL 끝을 `.md`로 바꾸면 마크다운으로 읽힌다(전체 인덱스: `/llms.txt`).
7. 작업이 일단락되면 `zisaze-toss/zisaze-toss_session_v(n).md` 세션문서를 남긴다(글로벌 지침 형식).

---

## 1. 목표 / 비목표

### 목표
- 토스 앱 안에서 동작하는 **"지사제" 비게임 미니앱**을 신규 구축해 검수 통과·출시한다.
- 기능 범위(MVP): 지원사업 **목록·검색·필터 / 맞춤 추천 / 상세(AI 요약 표시) / 원문 열기 / 북마크 / 프로필(관심정보) 설정 / 데이터 삭제**.
- 데이터·비즈니스 로직은 **기존 rich1 API를 그대로 재사용**한다(백엔드 신규 구축 없음).

### 비목표 (1차 출시에서 제외 — 이유 명기)
| 제외 항목 | 이유 |
|---|---|
| 리치앤택스 배너(ad_banners) | 토스 정책 위반(자사 서비스 유도 금지·외부 광고 금지). 사용자 승인 완료 |
| 웹푸시 알림 | 토스 WebView에서 서비스워커 푸시 작동 불가. 대체재(스마트 발송)는 Phase 5 |
| AI 상담(chat)·AI 탐색(ai-search) | SSE·비용·AI 고지 요건이 커서 후속 단계로. 토스는 "기존 기능 전부 이식 불필요, 순차 업데이트 가능" 명시 |
| 자체 로그인/회원가입 화면 | 토스 정책상 자사 로그인 제공 금지. 익명 식별키로 대체(§2 A3) |
| 결제·광고 수익화 | 현재 웹에도 없음. 해당 없음 |
| 관리자 기능 | 웹 admin으로 충분 |

> ⚠️ 콘솔 앱 설명에는 **MVP에 실제 들어가는 기능만** 기술할 것. 검수 항목에 "등록된 모든 서비스 기능 동일 제공"이 있어, 설명에 있는데 앱에 없으면 반려 사유가 된다.

---

## 2. 아키텍처 결정 (ADR)

### A1. 미니앱은 신규 독립 프로젝트 `c:\()안티그래비티\zisaze-toss\`
- 스택: **Vite + React 19 + TypeScript + `@apps-in-toss/web-framework`(SDK 2.4.5 이상)**. 순수 CSR.
- 산출물 `.ait` 번들을 토스 콘솔에 업로드하면 **토스 CDN이 호스팅**한다(자체 서버에서 프론트를 서빙하지 않음). 기존 URL 연결 방식은 앱인토스에 존재하지 않음(공식 튜토리얼 확인).
- 부트스트랩은 공식 스캐폴드(`npx create-ait-app@latest` → WebView 템플릿 선택) 우선. 실패 시 튜토리얼(tutorials/webview) 수동 셋업.
- 별도 git 저장소로 관리(비공개 `King-cheolhee/zisaze-toss` 권장). rich1 저장소에 섞지 않는다.
- 이유: rich1은 SSR(ISR)+API 라우트 62개로 서버 실행이 필수라 토스에 그대로 못 올림(SSR은 검수 금지 항목이기도 함). 별도 앱이 웹 무중단 원칙에도 부합.

### A2. 백엔드는 기존 rich1 API를 크로스오리진 호출
- 미니앱 → `https://www.zisaze.com/api/*` 직접 호출.
- **핵심 근거(조사 완료)**: rich1의 사용자 API는 전부 `Authorization: Bearer <JWT>` 헤더 인증이며 쿠키를 쓰지 않는다 → iOS 서드파티 쿠키 차단과 무관하게 그대로 재사용 가능.
- 필요한 rich1 변경은 CORS 허용뿐(§5 P1-1).

### A3. 사용자 식별 = `getAnonymousKey` (토스 로그인 없이)
- 비게임 미니앱 전용 API. **동의 화면·콘솔 설정·서버(mTLS) 구축 전부 불필요**. SDK 2.4.5+.
- 반환: `{ type: 'HASH', hash: string }` — 해당 미니앱에서만 유효한 사용자 고유 식별자.
- 흐름: 미니앱 최초 진입 → `getAnonymousKey()` → rich1 신규 엔드포인트 `POST /api/toss/session`에 hash 전달 → 서버가 users 행을 찾거나 생성 → **기존과 동일한 JWT 발급** → 이후 모든 기존 사용자 API(추천·북마크·프로필)가 무수정으로 동작.
- 토스 로그인(`appLogin`)을 안 쓰는 이유: mTLS 인증서 발급·서버 연동·동의 화면·약관 URL 등 부대작업이 크고, 지사제 미니앱에 필요한 건 "안정적 식별자"뿐. 정책상으로도 문제없음(로그인을 제공하려면 토스 로그인만 가능하다는 규정이며, 로그인 없는 식별키 방식은 비게임 표준 패턴). 업그레이드 경로는 Phase 5.
- ⚠️ 문서에 "기기 변경 시 동일 hash 유지" 여부가 명시되어 있지 않음 → **Phase 3 샌드박스/실기기에서 최우선 검증**(§7 R1). 샌드박스는 mock 값을 반환하므로 실기기 확인 필수.

### A4. rich1 변경은 딱 3가지 (전부 additive)
1. `src/middleware.ts`에 토스미니 도메인 CORS 허용 추가
2. `users` 테이블에 `toss_user_key` 컬럼 추가(ADD COLUMN, nullable, UNIQUE)
3. `POST /api/toss/session` 라우트 신설
→ 기존 웹 사용자·관리자 흐름에는 코드 경로가 전혀 겹치지 않는다.

### A5. 외부 이동은 전부 `openURL()`
- 원문보기(정부기관 공고), 개인정보처리방침 등 외부 URL은 `window.open`/`target="_blank"` 대신 SDK `openURL(url)` 사용(WebView에선 브라우저 탭, 기본 앱 전환).
- 정부 원문 이동은 토스 허용 예외("공공기관 공식 페이지 연결", "단순 정보 확인용 이동")에 해당 — 검수 메모에 명기(§5 P3-4).

### A6. 웹의 금지 요소는 미니앱에 "미포함"으로 처리
- GA(gtag)·카카오 공유 SDK·웹푸시(sw.js)·ad_banners·자체 로그인 화면 → 새 앱이므로 처음부터 넣지 않는다. 분석이 필요하면 `@apps-in-toss/web-analytics`만 사용.

---

## 3. 토스 하드 제약 (위반 = 검수 반려. 코드 작성 시 상시 준수)

| # | 제약 | 이 프로젝트에서의 적용 |
|---|---|---|
| H1 | SSR 금지, CSR/SSG만 | Vite CSR — 구조적으로 충족. Next.js 서버 기능 사용 금지 |
| H2 | `eval`·`new Function`·외부 코드 실행 금지 | 자체 코드 금지 + 빌드 산출물 grep 검사(P2-8). 외부 스크립트 태그 삽입 금지 |
| H3 | history.pushState/replaceState 직접 조작 금지 | 라우터 추상화만 사용 |
| H4 | 라이트 모드 고정(다크모드 미지원) | 다크 분기 코드 자체를 안 만듦 |
| H5 | 제스처 핀치줌 비활성화 | viewport meta `maximum-scale=1, user-scalable=no` |
| H6 | 인터랙션 반응 2초 이내, 진입 10초 이내 | 목록 페이지네이션·경량 카드 필드(fields=card) 사용. 자산 총량 극소(웹 public 기준 ~180KB) |
| H7 | 번들 압축해제 100MB 이하 | 이 앱 규모에서는 여유. 빌드 후 크기 기록 |
| H8 | 토스 제공 내비게이션 바 사용, 자체 헤더+뒤로가기 중복 금지 | 자체 상단 헤더/뒤로가기 버튼 미구현. 브랜드 로고·한글명은 콘솔/granite.config로 |
| H9 | 다크패턴 금지(진입 즉시 바텀시트·강제 유도·모호한 CTA) | 온보딩도 "건너뛰기" 가능하게. 진입 자동 팝업 금지(AI 고지는 §5 P2-7 방식) |
| H10 | 자사 서비스/앱 설치 유도 금지 | zisaze.com 웹·리치앤택스로의 유도 링크 일절 금지 |
| H11 | 외부 링크는 예외 사유 내에서만 | 정부 원문(공공기관)·법적 고지만. 그 외 외부 이동 없음 |
| H12 | 공유 기능은 `intoss://` 스킴 | 공유 넣을 경우 `getTossShareLink` 사용(MVP 선택) |
| H13 | WebSocket은 wss만 / HTTPS만 | 해당 없음(REST만) / API는 이미 HTTPS |
| H14 | 로그인 제공 시 토스 로그인만 | 로그인 화면 자체가 없음(익명 키) |
| H15 | UX 라이팅 해요체, 다이얼로그 좌측 버튼 '닫기' | 전 문구 해요체로 작성 |
| H16 | 생성형 AI 사용 고지 + 결과물 라벨 | AI 요약·AI 추천 영역에 "AI" 라벨 + 최초 상호작용 시 고지 1회(§5 P2-7). 법적 의무(위반 시 과태료) |
| H17 | 개인정보 최소수집·탈퇴(삭제) 수단 제공 | 사업자번호 등은 선택 입력으로. "내 데이터 삭제" 기능 필수(P2-7) |

**셀프체크 트리거**: 미니앱 코드를 수정할 때마다 ① SSR/서버 의존 추가 안 됨? ② eval/외부 스크립트 없음? ③ history 직접 조작 없음? ④ 다크 분기 없음? ⑤ 외부 이동은 openURL+허용 예외뿐? — 5문항을 확인하고 결과를 보고에 포함한다.

---

## 4. 시스템 구성

```
[토스 앱 (Android 7+/iOS 16+, 만 19세+)]
   └─ 지사제 미니앱 (.ait 번들, 토스 CDN 호스팅, WebView/CSR)
        │  getAnonymousKey() → hash
        │  fetch https://www.zisaze.com/api/*  (Authorization: Bearer JWT)
        ▼
[기존 rich1 서버 — AWS Lightsail + Nginx + PM2 + Cloudflare]  ← 변경 3종만(additive)
   ├─ (신설) POST /api/toss/session   : hash → users upsert → JWT 발급
   ├─ (수정) src/middleware.ts        : 토스미니 2개 오리진 CORS 허용
   └─ (기존 재사용) /api/programs, /api/recommend, /api/bookmarks, /api/users/me, /api/hidden …
        ▼
[Supabase PostgreSQL]  ← users에 toss_user_key 컬럼만 추가
```

미니앱이 호출하는 API 계약(전부 기존, 신설 1개):

| API | 용도 | 인증 |
|---|---|---|
| `POST /api/toss/session` **(신설)** | hash → `{ success, user, token }` (기존 login 응답과 동일 형태) | 없음(hash가 자격) |
| `GET /api/programs?fields=card&…` | 목록/검색/필터 (경량 필드) | 불필요 |
| `GET /api/programs/[id]` | 상세(AI 요약 포함) | 불필요 |
| `GET /api/recommend` | 맞춤 추천 | Bearer |
| `GET/POST/DELETE /api/bookmarks`, `GET /api/bookmarks/ids` | 북마크 | Bearer |
| `GET/PUT /api/users/me` | 프로필 조회/수정 | Bearer |
| `GET/POST /api/hidden` | 관심없음(선택 기능) | Bearer |
| `DELETE /api/toss/session` 또는 기존 탈퇴 API | 데이터 삭제 | Bearer |

> 구현 전 각 라우트의 실제 요청/응답 스키마를 코드에서 확인할 것(온보딩 문서·`rich1/src/app/api/**`). 응답 구조를 임의로 바꾸지 말 것(웹 프론트와 공유).

---

## 5. 단계별 실행 계획

> 표기: [사용자] = 사람이 직접, [AI] = 실행 에이전트. 각 태스크는 **완료 기준(AC)** 을 만족해야 다음으로 진행.

### Phase 0 — 사전 준비·결정 (개발 착수 전)

| ID | 작업 | 담당 | 완료 기준 |
|---|---|---|---|
| P0-1 | 토스 비즈니스 가입 → 앱인토스 콘솔 워크스페이스 생성(사업자당 1개 — 게임 프로젝트용으로 이미 만들었으면 그것을 공용) | [사용자] | 콘솔 접속 가능 |
| P0-2 | 앱 등록(비게임): 한글명 "지사제", **appName 확정(후보: `zisaze`) — 등록 후 수정 불가**, 카테고리, 고객센터 정보(`contact@zisaze.com`), 사용 연령 확인 | [사용자]+[AI 보조] | 콘솔에 앱 생성됨 |
| P0-3 | **개인정보처리방침·이용약관 페이지 신설** — 현재 웹에 없음(조사 확인). rich1에 정적 페이지 2개 추가(`/privacy`, `/terms` — additive라 §0-1 예외 아님, 단 문안은 사용자 검토 후 게시). 미니앱 수집 항목(토스 익명 식별자, 선택 프로필 정보) 반영 | [AI]→[사용자 검토] | zisaze.com/privacy 접속 가능. 웹 기존 화면 무영향 |
| P0-4 | 브랜드 자산 규격 제작: 로고 600×600 PNG(투명 불가·각진 정사각), 썸네일 1000×1000·1932×828, 스크린샷 세로 636×1048 ≥3장(개발 후 캡처) | [AI 초안]→[사용자 확정] | 콘솔 업로드 규격 충족 |
| P0-5 | `zisaze-toss` git 저장소 생성(비공개) | [AI] | 초기 커밋 push |

### Phase 1 — rich1 서버 준비 (additive 3종 + 배포)

| ID | 작업 | 상세 | 완료 기준 |
|---|---|---|---|
| P1-1 | CORS 허용 | `rich1/src/middleware.ts`에 추가: Origin이 `https://<appName>.apps.tossmini.com` 또는 `https://<appName>.private-apps.tossmini.com`(QR 테스트용)과 **정확히 일치**할 때만 `Access-Control-Allow-Origin`(오리진 에코)·`Allow-Headers: Authorization, Content-Type`·`Allow-Methods` 부여 + OPTIONS preflight 204 응답. 대상 경로는 미니앱이 쓰는 사용자 API로 한정(`/api/admin/*` 제외). 기존 관리자 인증 로직은 손대지 않음 | 로컬에서 preflight 시뮬레이션 통과 + 기존 웹(무 Origin/자기 오리진) 요청에 응답 헤더 변화 없음 확인 |
| P1-2 | DDL | `ALTER TABLE users ADD COLUMN toss_user_key text UNIQUE;` (nullable). 방법: `SUPABASE_DB_URL` pg 직결(8차 작업에서 검증된 경로, psql 없음 주의). **실행 전 사용자 승인 필수(DB 변경)** | 컬럼 존재 + 기존 행 영향 0 |
| P1-3 | `POST /api/toss/session` 신설 | 입력 `{ anonymousKey: string }` → 검증(길이·형식) → `users`에서 `toss_user_key=hash` 조회, 없으면 생성(이메일 NOT NULL 제약이면 합성 이메일 `toss_<hash앞12자>@miniapp.zisaze.com` — 구현 전 스키마 확인, `status='active'`) → 기존 `createToken()`으로 JWT 발급 → `{ success, user, token }` 반환(login 라우트와 동일 형태). 간단한 rate limit(IP당 분당 상한) 포함. `DELETE`(Bearer 필요)로 해당 유저 데이터 삭제(탈퇴) 지원 — 기존 탈퇴 API가 있으면 재사용 | 신규 hash로 user 생성·JWT로 `/api/users/me` 조회 성공. 동일 hash 재호출 시 같은 user. 기존 이메일 로그인 회귀 무영향 |
| P1-4 | 배포·라이브 검증 | rich1 표준 배포(SCP+dos2unix, `.git` 없음 주의, 배포 전 크롤러 status idle 확인). curl로 ①tossmini 오리진 preflight ②session 발급 ③기존 웹 로그인/홈 회귀 스팟체크 | 3항목 증거(응답 로그) 확보 |

> P1은 전부 뒤에서 미니앱이 없어도 무해한 변경이다(엔드포인트는 호출 전까지 불활성, CORS는 특정 오리진에만 반응).

### Phase 2 — 미니앱 MVP 개발 (`zisaze-toss/`)

| ID | 작업 | 상세 | 완료 기준 |
|---|---|---|---|
| P2-1 | 부트스트랩 | `npx create-ait-app@latest`(WebView/React) → `granite.config.ts`: appName(P0-2 확정값)·displayName "지사제"·primaryColor(웹 브랜드 블루 hex를 rich1 스타일에서 추출)·icon. viewport 줌 비활성. 라이트모드 고정 | `ait dev`(또는 스캐폴드가 안내하는 dev 명령) 구동 + 샌드박스 QR 접속 확인 |
| P2-2 | API 클라이언트 | `src/lib/api.ts`: BASE=`https://www.zisaze.com`(환경변수로 분리), JWT는 SDK Storage(또는 localStorage)에 저장, 401 시 세션 재발급 1회 재시도. **주의: Storage는 앱 삭제 시 소멸 → 토큰은 언제든 hash로 재발급 가능하므로 문제없음(설계 확인 사항으로 기록)** | 목록 API 실호출 성공(개발 환경) |
| P2-3 | 온보딩 | 최초 진입: `getAnonymousKey()` → `/api/toss/session` → 토큰 저장. 프로필(지역·업종·기업규모 등, 웹 EditProfileModal 필드 준용) 미설정이면 설정 유도 화면 — **단 "나중에 하기"로 건너뛰기 가능(H9), 사업자번호는 선택 입력(H17)** | 신규 사용자가 온보딩 완료/건너뛰기 양쪽 모두로 홈 도달 |
| P2-4 | 홈 | ① 맞춤 추천 캐러셀(`/api/recommend`, 프로필 없으면 미노출) ② 전체 목록(`/api/programs?fields=card` 페이지네이션) ③ 카테고리·지역 필터 ④ 검색 ⑤ 마감임박 정렬. 마감 D-day 표기는 웹 로직 준용 | 각 기능 실데이터로 동작. 첫 화면 로딩 3초 내(측정 기록) |
| P2-5 | 상세 | `/api/programs/[id]` — 제목·기관·기간·대상·요약. **AI 요약 영역에 "AI 요약" 라벨(H16)**. [원문 보기] = `openURL(detail_url)`. 북마크 토글 | 상세→원문 이동 정상. 라벨 노출 |
| P2-6 | 북마크·(선택)관심없음 | 북마크 목록 화면 + 카드 토글. 여유 시 '관심없음'(hidden) 연동 | 토글 후 재접속 시 유지(서버 저장 확인) |
| P2-7 | 설정 | ① 프로필 수정 ② **AI 활용 고지**: AI 추천/요약을 처음 접하는 시점에 1회 안내(진입 즉시 자동 바텀시트 금지 — 해당 영역 최초 노출 시 인라인/1회성 안내로) ③ **내 데이터 삭제**(확인 다이얼로그 → DELETE 세션 → 로컬 토큰 파기) ④ 개인정보처리방침·이용약관 링크(openURL) ⑤ 고객센터 안내 | 삭제 후 재진입 시 새 사용자로 시작됨 |
| P2-8 | 품질 게이트 | ① 빌드 산출물에서 `eval(`·`new Function` grep 0건(H2) ② 번들 압축해제 크기 기록(H7) ③ 전 문구 해요체 검수(H15) ④ §3 셀프체크 전항 통과 ⑤ 느린 3G 스로틀에서 진입 10초 내 | 체크 결과를 세션문서에 기록 |

### Phase 3 — 샌드박스 QA·검수 준비

| ID | 작업 | 담당 | 완료 기준 |
|---|---|---|---|
| P3-1 | 자가 체크리스트 전수 점검 — §8 표를 하나씩 확인·기록 | [AI] | 전항 ✅ 또는 사유 기록 |
| P3-2 | 샌드박스(테스트앱) 테스트: 콘솔에서 번들 업로드 → QR → 사용자 토스앱으로 전 화면·전 흐름. ⚠️ 샌드박스의 `getAnonymousKey`는 mock 반환 → 세션 발급은 `private-apps` 도메인 CORS 포함 실서버로 검증 | [사용자]+[AI] | 전 흐름 통과. 발견 버그 수정 |
| P3-3 | **실기기 검증(핵심)**: ① Android·iOS 각 1대 ② **같은 토스 계정으로 기기/재설치 간 hash 동일성 확인(§7 R1 게이트)** ③ iOS에서 줌·Safe Area·뒤로가기 | [사용자]+[AI] | hash 영속 확인. 불일치 시 §7 R1 대응 발동 |
| P3-4 | 검수 메모 작성: "원문 보기는 정부·공공기관 공고 원문 확인용 외부 이동(허용 예외 해당)", "AI 요약·추천에 AI 라벨·고지 적용", "로그인 미제공·익명 식별키 사용" 등 심사관 오해 소지 선제 설명 | [AI] | 콘솔 제출용 텍스트 완성 |
| P3-5 | 콘솔 정보 최종화(설명=MVP 기능만·스크린샷·키워드) → **검토 요청 클릭** | [사용자] | 제출 완료(영업일 2~3일 소요, 반려 시 사유 분석→수정→재제출 반복) |

### Phase 4 — 출시·운영

- [사용자] 승인 후 "출시하기" — **즉시 100% 반영**되므로 P3 전 항목 완료 후에만.
- 운영: 콘솔 지표 모니터링, 반려/신고 대응은 채널톡. 서버측은 기존 rich1 모니터링에 편승(`/api/toss/session` 에러 로그 확인 항목 추가).
- 미니앱 수정 배포도 매번 검수 재통과 필요(핫픽스는 채널톡 문의 경로).

### Phase 5 — 후속 확장 (별도 승인 후)

1. **AI 탐색 이식**(+AI 고지 강화, API 비용 검토) 2. **AI 상담(chat)** — SSE가 WebView에서 정상인지 선검증 3. **스마트 발송**(신규사업 알림 대체 — 수신동의·템플릿 검수 2~3일·문구 제약) 4. **토스 로그인 전환**(mTLS 서버 연동 — R1 실패 시 앞당김) 5. 공유하기(`getTossShareLink`).

---

## 6. 데이터 설계

- `users.toss_user_key text UNIQUE NULL` — 미니앱 사용자만 값 보유. 기존 웹 사용자는 NULL(영향 없음).
- 미니앱 사용자 행: `email`은 스키마가 NOT NULL이면 합성 이메일(`toss_<hash12>@miniapp.zisaze.com`), `password_hash`는 임의 난수(로그인 불가 값), 프로필 필드는 온보딩 입력값.
- 기존 테이블 재사용: `bookmarks`, `hidden`, `users.preference_*`(추천 개인화) — 전부 user_id FK 기반이라 무수정 동작 예상(구현 시 각 라우트에서 확인).
- 삭제(탈퇴): 해당 user 행 + 연관 행(bookmarks/hidden 등) 삭제. 개인정보보호법 "지체 없이 파기" 충족.
- 웹 계정 ↔ 미니앱 계정 연동은 하지 않음(YAGNI — 필요해지면 Phase 5에서 설계).

## 7. 리스크 & 대응

| ID | 리스크 | 가능성/영향 | 대응 |
|---|---|---|---|
| R1 | `getAnonymousKey`의 기기 간/재설치 간 영속성이 문서에 미명시 | 중/높음 | P3-3에서 실기기 게이트 검증. 불일치 시: 단기=Storage 병행 저장으로 재매핑, 근본=토스 로그인(appLogin) 전환(Phase 5 앞당김). 개발 초기(P2-3)에 커뮤니티/채널톡으로도 확인 권장 |
| R2 | 라이브 CORS가 테스트와 다를 수 있음(토스 문서 명시) | 중/중 | 출시 직후 라이브 도메인에서 API 스모크 테스트 |
| R3 | Cloudflare가 tossmini 오리진 요청을 차단(봇 판정 등) | 낮음/중 | P1-4 curl 검증에 Origin 헤더 포함. 문제 시 Cloudflare 규칙에 예외 추가 |
| R4 | 심사관이 정부 원문 외부 이동을 "외부 의존"으로 해석 | 낮음/중 | 검수 메모(P3-4)로 선제 설명: 핵심 기능(검색·추천·요약·북마크)은 앱 내 완결, 원문은 정보 확인용 |
| R5 | 익명 hash 스푸핑(타인 hash를 알면 그 사용자의 북마크 조작 가능) | 낮음/낮음(민감정보 아님) | hash는 외부 유출 경로 없음 + rate limit + 저장 데이터 최소화. 민감 기능 추가 시 토스 로그인 전환 전제 |
| R6 | appName 오타·후회 (수정 불가) | -/높음 | P0-2에서 사용자 최종 확정 절차 명문화 |
| R7 | 검수 반려 라운드로 일정 지연(라운드당 2~3영업일) | 중/중 | §8 자가 체크 전수 + 다크패턴·내비바(반려 1순위) 중점 확인 |
| R8 | SDK/문서 변경(수시 개정) | 중/낮음 | §0-6 규칙: 공식 문서 정본, 착수 시 재확인 |

## 8. 검수 전 자가 체크리스트 (비게임 공식 항목 반영)

**접속·기능**: □ 정상 진입(10초 내) □ 콘솔에 등록한 기능 전부 동작 □ 앱 스킴(intoss://appName) 정상 □ 안드로이드 하드웨어 뒤로가기 정상
**내비게이션 바**: □ 비게임 내비바 사용 □ 자체 헤더/뒤로가기 중복 없음 □ 브랜드 로고+한글명 표시 □ 우측 기능 버튼 ≤1
**사용자 식별·데이터**: □ 식별자 확인·저장 □ 재접속 시 데이터 유지 □ 기기 변경 시 유지(R1)
**보안**: □ eval 등 외부 코드 실행 없음 □ history 조작 없음 □ SSR 아님(CSR) □ wss만(해당 없음)
**서비스 동작**: □ 제스처 줌 비활성 □ 라이트 모드 □ 2초 내 반응 □ 외부 링크 정상(openURL) □ 비속어/선정 콘텐츠 없음 □ 네트워크/메모리 급증 없음
**UX/다크패턴**: □ 진입 즉시 바텀시트 없음 □ 강제 유도 구조 없음 □ 모든 화면에서 나가기 가능 □ CTA 명확 □ 자사 유도 없음
**법적**: □ AI 고지·라벨(H16) □ 개인정보처리방침 접근 가능 □ 데이터 삭제 기능 □ 최소수집(사업자번호 선택화)
**콘솔**: □ 설명=실기능 일치 □ 스크린샷 규격 □ 고객센터 정보

## 9. 사용자(사람) 직접 작업 목록 (시간순)

1. [P0] 토스 비즈니스 가입·워크스페이스·앱 등록, **appName 최종 확정**
2. [P0] 개인정보처리방침 문안 검토·승인
3. [P1-2] users 테이블 DDL 실행 승인
4. [P0/P3] 로고·썸네일 확정, 스크린샷 선택
5. [P3] 본인 토스앱으로 샌드박스 QR 테스트, 실기기 2대 검증(특히 R1)
6. [P3-5] 검토 요청 클릭 / [P4] 출시하기 클릭
7. (선택) 채널톡 사전 문의: getAnonymousKey 영속성, 정부 원문 외부링크 예외 해당 여부

## 10. 예상 공수 (참고)

- Phase 0~1: 1~2일(콘솔 대기 별도) / Phase 2: 3~5일 / Phase 3: 2~3일 + 검수 2~3영업일(반려 시 라운드 추가) → **순작업 약 1.5~2주 + 검수 대기**. 토스 공식 안내(비게임 1~3개월)보다 짧은 이유: 백엔드·데이터·로직을 전부 재사용하고 화면만 새로 만들기 때문.

## 11. 오픈 이슈 (개발 착수 전 사용자 확정 필요)

| # | 항목 | 권장안 |
|---|---|---|
| O1 | appName(영문, 불변) | `zisaze` |
| O2 | 미니앱 한글명 | `지사제` |
| O3 | 온보딩에서 사업자번호 수집 여부 | 선택 입력(미입력도 추천 동작 — 지역·업종만으로) |
| O4 | MVP 범위(§1 비목표 승인 — AI탐색·상담·알림 제외) | 승인 요청 |
| O5 | '관심없음'(hidden) 포함 여부 | 여유 시 포함(P2-6) |

---

## 부록 — goal 모드 실행용 킥오프 프롬프트 (복사해서 사용)

```
지사제를 토스 미니앱(앱인토스)으로 출시하는 프로젝트를 시작한다.
c:\()안티그래비티\zisaze-toss\구현계획서_v1.md 가 이 프로젝트의 정본 계획서다. 전체를 정독한 뒤,
§0 실행 규칙을 준수하면서 Phase 0의 [AI] 작업부터 순서대로 진행해라.
- rich1(기존 웹) 변경은 계획서 §5 Phase 1의 3종 외 금지. rich1 작업 전 rich1/docs/AI_ONBOARDING.md 필독.
- 각 태스크는 완료 기준(AC)을 증거와 함께 보고하고, [사용자] 표시 작업과 §11 오픈 이슈는 나에게 물어라.
- 토스 콘솔의 검토 요청/출시 버튼은 절대 네가 진행하지 마라.
```

> 참고 원문: 앱인토스 제약 총정리 = `c:\()안티그래비티\hamtopia\docs\앱인토스_제약사항.md` (게임 중심이지만 공통 제약 포함), 가이드 정리 = `c:\()안티그래비티\탱탱볼해금\앱인토스_게임개발_가이드_정리.md`, 비게임 워크플로 베이스 = `c:\()안티그래비티\탱탱볼해금\APPS_IN_TOSS_WORKFLOW.md`
